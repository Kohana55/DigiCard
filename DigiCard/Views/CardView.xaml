<UserControl x:Class="DigiCard.Views.CardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DigiCard.Views"
             xmlns:controls="clr-namespace:DigiCard.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!--Left: MENU-->
        <controls:MenuView x:Name="_MenuView" Grid.Column="0"/>
        
        <!--Right: VIEWS-->
        <controls:HomeView x:Name="_HomeView" Grid.Column="1"/>
        <controls:BioView x:Name="_BioView" Grid.Column="1"/>
        <controls:SkillsView x:Name="_SkillsView" Grid.Column="1"/>
        <controls:ProfessionalView x:Name="_ProfessionalView" Grid.Column="1"/>
        <controls:KataView x:Name="_KataView" Grid.Column="1"/>
        <controls:ContactView x:Name="_Contact" Grid.Column="1"/>
    </Grid>
</UserControl>
